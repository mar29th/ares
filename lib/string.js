define(['./type'], function(type) {
  'use strict';

  function sprintf(format, vals) {
    var i,
        format = arguments[0];
    for (i = 1; i < arguments.length; ++i) {

    }
  }

  return {
    sprintf: sprintf
  }
});